;===========================================================================
; gen3y.pro
;
; nx x ny x nz array with values incrementing in the y direction
;
;===========================================================================
function gen3y, nx, ny, nz
 return, reform((lindgen(ny*nx)/nx)#make_array(nz,val=1),nx,ny,nz,/over)
end
;===========================================================================
