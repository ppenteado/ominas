;============================================================================
; cm_image
;
;
;============================================================================
function cm_image, image, top=top, bottom=bottom, gamma=gamma

 cmd = colormap_descriptor(top=top, bottom=bottom, gamma=gamma)
 cm = compute_colormap(cmd)

 return, apply_colormap(image, cm)
end
;============================================================================
