;=============================================================================
; contiguous_indices.pro
;
;=============================================================================
function contiguous_indices, x

 n = n_elements(x)

 w = min(where(dindgen(n)+x[0] NE x))
 if(w[0] EQ -1) then return, x
 return, x[0:w[0]]
end
;=============================================================================
