;=============================================================================
;+
; NAME:
;	pg_points
;
;
; PURPOSE:
;	Concatenates the given pg_points_struct's into an array of image
;	points.
;
;
; CATEGORY:
;	NV/PG
;
;
; CALLING SEQUENCE:
;	result = pg_points(object_ps)
;
;
; ARGUMENTS:
;  INPUT:
;	object_ps:	Array of pg_points_struct.	
;
; RETURN:
;	Array (2,n) of image points.
;
;
; STATUS:
;	Complete
;
;
; SEE ALSO:
;	pg_vectors
;
;
; MODIFICATION HISTORY:
; 	Written by:	Spitale, 2/1998
;	
;-
;=============================================================================
function pg_points, object_ps, sample=sample, all=all, name=get_name
 if(NOT keyword__set(object_ps)) then return, 0
 pgs_points_ps, object_ps, points_ps=points_ps, flags_ps=flags_ps, name=names
 return, pgs_cat_points(points_ps, flags_ps=flags_ps, sample=sample, $
                                            names=names, get_name=get_name)
end
;=============================================================================
