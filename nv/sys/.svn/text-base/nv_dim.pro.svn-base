;=============================================================================
; nv_dim
;
;=============================================================================
function nv_dim, ddp
@nv.include
 nv_notify, ddp, type = 1
 dd = nv_dereference(ddp)

 if(NOT ptr_valid(dd.dim_p)) then return, 0

 return, *dd.dim_p
end
;=============================================================================
