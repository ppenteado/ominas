; _str_set.include
;  implemented as a .include because arg_present does not propogate to 
;  lower-level calls

 if(defined(lum)) then cam_set_lum, _xd, lum, /noevent
 if(defined(sp)) then cam_set_sp, _xd, sp, /noevent

@glb_set.include
