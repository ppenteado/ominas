; rng_query.include
;  implemented as a .include because arg_present does not propogate to 
;  lower-level calls

 if(arg_present(primary)) then primary = rng_primary(_xd, cat=cat, condition=condition, /noevent)
 if(arg_present(desc)) then desc = rng_desc(_xd, cat=cat, condition=condition, /noevent)

@dsk_query.include

