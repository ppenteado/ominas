; _rng_assign.include
;  implemented as a .include because arg_present does not propogate to 
;  lower-level calls

 if(defined(primary)) then rng_set_primary, _xd, primary, /noevent
 if(defined(desc)) then rng_set_desc, _xd, desc, /noevent

@dsk_assign.include
