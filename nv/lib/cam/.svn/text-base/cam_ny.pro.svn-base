;===========================================================================
; cam_ny
;
;
;===========================================================================
function cam_ny, cxp
@nv_lib.include
 cdp = class_extract(cxp, 'CAMERA')
 nv_notify, cdp, type = 1
 cd = nv_dereference(cdp)
 return, (cd.size)[1,*]
end
;===========================================================================



