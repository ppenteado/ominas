;===========================================================================
; image_azimuth_pos
;
;  Computes azimuth angle of projection of inertial position vector v into 
;  the image plane.  When the image is displayed with (0,0) at the top-left, 
;  the azimuth angle is measured counterclockwise from "up".
;
;===========================================================================
function image_azimuth_pos, cd, v

 vv = bod_inertial_to_body_pos(cd, v)

 azimuth = -atan(vv[0], -vv[2])

 return, azimuth
end
;===========================================================================



