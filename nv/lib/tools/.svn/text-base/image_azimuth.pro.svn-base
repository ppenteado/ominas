;===========================================================================
; image_azimuth
;
;  Computes azimuth angle of projection of inertial vector v into image 
;  plane.  When the image is displayed with (0,0) at the top-left, the 
;  azimuth angle is measured counterclockwise from "up".
;
;===========================================================================
function image_azimuth, cd, v

 vv = bod_inertial_to_body(cd, v)

 azimuth = -atan(vv[0], -vv[2])

 return, azimuth
end
;===========================================================================



