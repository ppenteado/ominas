;===========================================================================
; rng_primary
;
;
;===========================================================================
function rng_primary, rxp
@nv_lib.include
 rdp = class_extract(rxp, 'RING')
 nv_notify, rdp, type = 1
 rd = nv_dereference(rdp)
 return, rd.primary
end
;===========================================================================



