;===========================================================================
; rng_desc
;
;
;===========================================================================
function rng_desc, rxp
@nv_lib.include
 rdp = class_extract(rxp, 'RING')
 nv_notify, rdp, type = 1
 rd = nv_dereference(rdp)
 return, rd.desc
end
;===========================================================================



