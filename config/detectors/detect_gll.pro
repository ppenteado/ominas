;===========================================================================
; detect_gll.pro
;
;===========================================================================
function detect_gll, label, udata

 if( (strpos(label, 'SSI') NE -1) AND $
          (strpos(label, 'GALILEO') NE -1) ) then return, 'GLL_SSI'

 return, ''
end
;===========================================================================
