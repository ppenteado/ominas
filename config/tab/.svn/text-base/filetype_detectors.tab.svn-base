# The first filetype in this list is treated as a default.
# detector fn			filetype	action
# (returns T/F)

  detect_dh                     DH		ignore
  detect_vicar			VICAR		read
  detect_occ			OCC		read
  detect_fits			FITS		read
  detect_vgr_cd			VGR_CD		read
  detect_tdl			TDL		read
  detect_multi			MULTI		read
  detect_pds			PDS		read


