; _str_assign.include
;  implemented as a .include because arg_present does not propagate to 
;  lower-level calls

 if(defined(lum)) then str_set_lum, _xd, lum, /noevent
 if(defined(sp)) then str_set_sp, _xd, sp, /noevent

@glb_assign.include
