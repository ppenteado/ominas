;===========================================================================
; wrap_range
;
;===========================================================================
function wrap_range, x, max 

 xx = x

 m = xx mod max

 w = where(xx GT max)
 if(w[0] NE -1) then xx[w] = m[w]

 return, xx
end
;===========================================================================
