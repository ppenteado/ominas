;=============================================================================
; extend_mask
;
;
;=============================================================================
function extend_mask, _mask, dist

 mask = double(_mask)
 mask = smooth(mask, dist)
 w = where(mask NE 0)
 if(w[0] NE -1) then mask[w] = 1

 return, byte(mask)
end
;=============================================================================
