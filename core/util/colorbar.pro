;======================================================================================
; colorbar
;
;======================================================================================
pro colorbar, colors

 nc = float(n_elements(colors))

 plot, /norm, /nodata, [0], [0]
 for i=0, nc-1 do $
  oplot, [i/nc, (i+1)/nc], [0, 0], th=10, col=colors[i]

end
;======================================================================================
