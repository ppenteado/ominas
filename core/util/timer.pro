;============================================================================
; timer
;
;
;============================================================================
pro timer, s, t0=t0
 
 if(NOT defined(t0)) then t0 = 0d

 t = systime(/sec)
 dt = t-t0
 t0 = t
 if(keyword_set(s)) then print, s, dt
end
;============================================================================
