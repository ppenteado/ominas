;====================================================================================
; maxdev
;
;====================================================================================
function maxdev, x

 nx = n_elements(x)

 M = x#make_array(nx, val=1d)

 return, max(abs(M - transpose(M)))
end
;====================================================================================
